apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: xwiki-data
  namespace: xwiki
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 8Gi
